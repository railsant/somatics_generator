<h3><%%= "#{t('Bulk')} #{t('Update')}" %></h3>
<div class="issue"> 
  <table width="100%">
    <tbody>
      <!-- <tr>
        <td><b>Field</b></td>
        <td><%%#= text_field_tag '<%= singular_name %>[field]' %></td>
      </tr> -->
    </tbody>
  </table>
</div>
<%%= submit_tag("#{t('Bulk')} #{t('Update')}") %>
